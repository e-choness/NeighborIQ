/** Shared input styling for native <input>/<select> (keeps them accessible and stylable). */
export const inputClass =
  'h-10 w-full rounded-lg border border-border bg-surface-2 px-3 text-sm text-text placeholder:text-muted outline-none transition-colors focus:border-accent'
