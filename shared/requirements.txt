# Runtime dependencies of shared/ (models, database sessions, analytics).
# Installed by every image that copies shared/. Dependabot proposes updates weekly.
sqlalchemy[asyncio]==2.0.54
asyncpg==0.31.0
psycopg2-binary==2.9.13
pydantic==2.13.5
